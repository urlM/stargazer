# Copy this file to .env.local to override defaults locally.
# .env.local is gitignored — never commit it.

# GITHUB_TOKEN=ghp_your_personal_access_token_here# Copy this file to .env.local to override defaults locally.
# .env.local is gitignored — never commit it.

# GITHUB_TOKEN=ghp_your_personal_access_token_here