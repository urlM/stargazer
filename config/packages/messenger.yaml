framework:
    messenger:
        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
        routing:
            App\Message\SyncRepositoriesMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
